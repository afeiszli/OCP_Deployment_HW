---
- name: Jenkins Setup
  hosts: localhost
  tasks:
    - name: jenkins setup
      script: ./appdev/cicd-setup.sh

- name: HPA configuration on production deployment of openshift-tasks
  hosts: localhost
  tasks:
#    - name: Create LimitRange
#      shell: oc create -f - ./appdev/hpa.yaml -n qa
#    - name: Autoscale tasks
#     shell: oc autoscale dc openshift-tasks --min 1 --max 3 --cpu-percent=85 -n qa

- name: Creation of users for Alpha and Beta clients

- name: Assigning labels to the users according to its group

- name: Setup the environment for Alpha and Beta clients


